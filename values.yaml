replicaCount: 2

image:
  repository: preranabl/sock-shop-fe
  tag: "0.2.0"
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80
  targetPort: 8079

ingress:
  enabled: true
  className: nginx
  host: sockshop.local
  path: /

securityContext:
  runAsUser: 10001
  runAsGroup: 10001
