apiVersion: v1
kind: Service
metadata:
  name: {{ include "sockshop-frontend.fullname" . }}
  labels:
    {{- include "sockshop-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sockshop-frontend.selectorLabels" . | nindent 4 }}
